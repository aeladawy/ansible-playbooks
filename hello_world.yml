- name: Hello World Sample
  hosts: all
  gather_facts: yes
  environment:
    ANSIBLE_CALLBACKS_ENABLED: ansible.posix.timer
  
  tasks:
    - name: Hello Message
      debug:
        msg: "{{ hello_message }}"
