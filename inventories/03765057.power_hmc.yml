# The most minimal example, targeting only a single HMC
plugin: ibm.power_hmc.powervm_inventory
use_extra_vars: true
hmc_hosts:
  - hmc: "{{ hmc }}"
    user: "{{ user }}"
    password: "{{ password }}"
